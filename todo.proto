syntax = "proto3";

service Todo {
  rpc AddTask(Task) returns (Task) {}
  rpc ListTasks(ListRequest) returns (Tasks) {}
}

message ListRequest {
  string title = 1;
}

message Task {
  string title = 1;
  string description = 2;
}

message Tasks {
  repeated Task tasks = 1;
}
